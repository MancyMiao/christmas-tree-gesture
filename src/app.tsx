import React from "react";
import GestureTree from "./components/GestureTree";  // 引入 GestureTree 组件

function App() {
  return (
    <div style={{ width: "100%", height: "100vh", background: "#000" }}>
      <GestureTree />
    </div>
  );
}

export default App;
